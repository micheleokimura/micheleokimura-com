# Wait list intake (/api/contact). Set these in Vercel -> Project -> Settings ->
# Environment Variables. Until RESEND_API_KEY is set, the wait list form falls
# back to a plain mailto so no signup is ever lost.

# Resend (emails each signup to Michele). https://resend.com
RESEND_API_KEY=
# Optional overrides. Defaults: WAITLIST_TO=michele@micheleokimura.com,
# WAITLIST_FROM="Michele Okimura <noreply@micheleokimura.com>".
# The FROM domain must be verified in Resend.
WAITLIST_TO=
WAITLIST_FROM=

# GoHighLevel (best-effort: creates a contact tagged "wait-list-2026").
# Both must be set for the CRM sync to run; the email send works without them.
GHL_API_KEY=
GHL_LOCATION_ID=

# Contact popup (the "Contact" button in the header). It posts to
# /api/contact-message, which emails Michele through Resend using the
# RESEND_API_KEY above. Nothing else is required to make the form work.
# Optional overrides. Defaults: CONTACT_TO=michele@micheleokimura.com,
# CONTACT_FROM="Website <onboarding@resend.dev>" (Resend's shared sender, which
# needs no DNS). Once micheleokimura.com is verified in Resend, set CONTACT_FROM
# to "Michele Okimura <noreply@micheleokimura.com>".
CONTACT_TO=
CONTACT_FROM=
